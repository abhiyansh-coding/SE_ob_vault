# Log

Append-only timeline. Newest at the bottom.
Format: `## [YYYY-MM-DD] <op> | <description>` — greppable via `grep "^## \[" wiki/log.md`.

## [2026-09-02] scaffold | Vault initialized on the CN/CAT wiki pattern: `raw/{sources,sources/ppts,submissions}` + `wiki/{topics,papers/worksheets}`, git repo, CLAUDE.md schema. The eight rules carried over intact from the CN vault — they govern where knowledge comes from, not any one subject. Three SE-specific additions to the schema: a Diagram answers section (legend → drawing → reading), because SE's distinctive answer type is a drawing graded on notation; a Definitions & distinctions variant of the Formulas part, for the pairs this subject examines precisely because they blur; and a note that story.md should be written off the lifecycle's built-in causation. Phase folders deliberately NOT created — they must come from the handout's own unit structure on ingest, not be guessed. No topic pages, no syllabus, no weightage: `raw/sources/` is empty and rules 1 and 2 have nothing to stand on yet.
